# Compile, Debug, Publish

## ??

- ?? Typst ?????????????????????????

## ????

- `typst/crates/typst-cli/src/args.rs`
- `typst/docs/tutorial/**`
- `typst/docs/guides/page-setup.md`
- `typst/docs/guides/accessibility.md`

## ???

1. ? `typst init` ????????????
2. ? `typst compile` ???????? `typst watch` ??????
3. ?? `--root`?`--input`?`--font-path`?`--package-path`?`--package-cache-path` ?????
4. ??????????????? `typst eval`??? `typst query` ??? CLI ???? deprecated?
5. ? `typst fonts` ? `typst info` ????????????
6. ????????????PDF / HTML / SVG / PNG?

## ????

- ?????????????????????
- ????????? `.typ` ???
- ???????????? `set page`?`show` ???`context/state/query`?
- ??????????????????????????

## ????

- ?????????????
- ? HTML / SVG / PNG / PDF ?????????????
- ? 0.14.x ??????? `query` CLI ????
